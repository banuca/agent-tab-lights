{
  "name": "agent-tab-lights",
  "version": "0.3.0",
  "description": "Chrome extension that shows a status light in the tab title while AI agents work.",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/banuca/agent-tab-lights.git"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "icons": "node tools/generate-icons.mjs",
    "package": "node scripts/package.mjs"
  }
}
