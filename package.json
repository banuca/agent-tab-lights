{
  "name": "agent-tab-lights",
  "version": "0.2.0",
  "description": "Chrome extension that adds a coloured status dot to agent tab titles.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\""
  }
}
